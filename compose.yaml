services:
  backend:
    build: ./backend
    env_file: ./backend/.env
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - ./backend:/app
      - /app/node_modules # keep using node_modules of the container to avoid host OS malfunctions

  frontend:
    build: ./frontend
    env_file: ./frontend/.env
    ports:
      - "127.0.0.1:5173:5173"